# LED Matrix ASCII Generator

Tool used to generate Arduino C++ source code for byte arrays to be displayed on an LED Matrix.

## Usage

Install the dependencies:

```sh
pip install -r requirements.txt
```

Start the Flask server:

```
flask run
```

Open the displayed URL in your web browser and you're good to go.
